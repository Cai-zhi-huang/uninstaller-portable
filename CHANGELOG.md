# 更新日志 (Changelog)

本文件记录「卸载管理器 (Uninstaller Manager)」各版本的改动说明。

---

## 卸载无命令条目提前拦截（R9）

- 单条卸载前若条目没有卸载命令（多为已判定残留、卸载程序已不存在的注册表项），不再走到“卸载失败”误报，而是友好提示「没有可用的卸载命令」并引导改用「删除注册表项 / 强制删除此条目」清理。

---


## 残留删除部分失败反馈（R8）

- 删除残留文件时若部分因占用/无权限失败，不再只弹笼统“失败”：重新扫描残留并提示「已删除 X 个，仍有 Y 个残留未删除（可能被占用或无权限）」或「已删除 X 个残留」，单条与批量删除一致。

---


## 搜索无结果状态栏明确提示（R7）

- 过滤后 0 匹配时，状态栏改为提示「未找到与『关键词』匹配的项（共 N 个软件，已全部被过滤隐藏）」或「没有残留项」，避免用户误以为扫描失败/列表为空；有匹配时仍显示匹配数与占用空间。

---


## 批量卸载单一总进度条（R6）

- `doUninstall` 新增 `showProgress` 参数：单条卸载仍弹独立进度框；批量卸载改为由调用方的单一总进度条逐条目推进（显示「正在卸载：X (n/N)」），不再每卸一项就闪一个独立对话框，体验更连贯。
- 批量卸载成功/取消/失败计数逻辑保持不变。

---


## 复制卸载命令改为规范化可运行命令（R5）

- 开发者菜单「复制卸载命令」现在复制 `getUninstallCommand` 规范化后的命令：MSI 项补全为 `msiexec.exe /x {GUID} /quiet /norestart`，普通程序用原始字符串，可直接粘贴到命令行/脚本执行，比原始 UninstallString 更实用。

---


## v0.0.2 (试用版) — 卸载取消不再误报失败（R4）

- 卸载结果改为三态（成功 / 失败 / 已取消）。用户拒绝 UAC 提权（`ERROR_CANCELLED`）或在 MSI 向导中点取消（退出码 1602）时返回「已取消」，提示「未做任何更改」，不再误报成「卸载失败」。
- 单条卸载取消后不重扫列表（无变化）；批量卸载独立统计「成功 / 已取消 / 失败」数量，取消项不再计入失败。

---


## v0.0.2 (试用版) — 搜索别名扩展（R3）

- 搜索别名表新增 13 组常用软件：网易云音乐、腾讯会议、企业微信、百度网盘、QQ音乐、飞书、原神、米哈游、喜马拉雅、知乎、微博、小红书等。
- 现在搜中文名（如「网易云音乐」）可命中 cloudmusic / netease，搜英文也能反查中文条目，互搜更全。

---


## v0.0.2 (试用版) — 运行中/残留检测更准确（R2）

- 扩展进程名关键字的中文→英文映射（企业微信、腾讯会议、网易云音乐、百度网盘、支付宝、抖音、飞书、B站、米哈游/原神、小红书、喜马拉雅、知乎、微博等）。
- 「运行中」标记与残留判定共用这套关键字，覆盖更多国产软件，减少误判残留 / 漏标运行中的情况。

---


## v0.0.2 (试用版) — 残留扫描防卡死（R1）

- 残留判定扫描安装目录前，先检查所在盘类型：网络盘 / 可移动盘 / U 盘 / CD 等非固定盘直接跳过，避免后台扫描在慢速或不可达路径上阻塞、拖垮整个列表加载。
- `dirContainsExe` 与 `dirContainsUninstaller` 同步增加盘类型护栏，行为一致。

---


## v0.0.2 (试用版) — 稳定性与体验修复

> 聚焦「列表真实反映状态、排序不被意外重置、发布一致性」：

### 列表与状态
- **运行中软件不再从列表消失**：原按独占分组过滤，会导致正在运行的软件整组被隐藏；现归入普通列表并以蓝色「运行中」标注。
- **空名称软件不再被隐藏**：原独占分组会被过滤隐藏、无法卸载；现以「(未命名软件)」占位符显示、可选中卸载。
- **卸载/删除后列表实时刷新**：修复缓存命中时（`built_list` 默认 `allowCache`）卸载或删除后列表不刷新的可见性 bug，操作后一律强制重扫。

### 排序与搜索
- **退出/重扫后保留排序偏好**：修复重扫（卸载/删除/刷新）后列表被强制重置回「名称升序」的问题，现保留用户设置的排序列与方向。
- **清空搜索框保留排序**：修复搜索词清空后排序被重置的问题，与上方同源修复。

### 性能与发布
- **进程快照只枚举一次**：并行扫描前只枚举一次系统进程名快照，各软件只读复用，避免数百次重复枚举。
- **卸载入口缺失不再误报失败**：卸载器未返回进程句柄时视为已发起、返回成功，避免误导与列表不刷新。
- **发布闭环四端对齐**：源码/构建/便携包/GitHub Release 资产强制 md5 一致，更新日志同步真实改动。

---

## v0.0.1 (试用版) — 功能增强

> 本次更新聚焦「更安全、更高效、更好用」，共 10 项改进：

### 安全与删除
- **① 残留文件送回收站**：删除卸载残留文件时优先通过 `SHFileOperationW` 送入回收站（可撤销），仅在回收站不可用时回退为直接删除，避免误删无法恢复。
- **⑦ 系统关键项保护**：对 `SystemComponent`、系统发行商（Microsoft / Windows / Intel / Realtek 等）及名称含 driver / update / KB / Visual C++ / .NET 的条目自动灰显并标注「系统关键」，卸载时拦截，防止误卸系统组件。

### 批量与效率
- **② 操作后自动刷新**：卸载成功或删除残留后自动重扫软件列表并刷新界面，无需手动重载。
- **③ 批量操作**：表格支持多选（Ctrl/Shift），新增「批量卸载」与「批量删残留」按钮，可一次性处理多个条目（含系统关键项过滤与统一预览）。
- **⑩ 扫描进度细化**：扫描时进度标签显示当前正在扫描的盘符（如 `[扫描 D:]`），多盘/网络盘场景更直观。

### 卸载体验
- **④ 卸载前预览**：卸载前弹出预览框，列出目标软件及预计可释放的空间，确认后再执行。
- **⑧ 注册表定位**：右键菜单新增「在注册表中定位」，自动写入 `LastKey` 并拉起 `regedit` 跳转到该软件对应的卸载项。

### 搜索与导出
- **⑤ 搜索增强**：搜索框支持前缀匹配，并新增**拼音首字母哈希**（如「微信」可命中 `wx`、`WeChat`），中英文别名更易搜到。
- **⑥ 清单导出**：软件清单支持导出为 **CSV（RFC4180 转义）/ HTML（表格）/ TXT** 三种格式，便于归档与比对。

### 外观
- **⑨ 亮/暗主题**：菜单新增「视图 → 亮色 / 暗色」主题切换，选择通过 `QSettings` 持久化，重启后保持。

---

## v0.0.1 (试用版) — 早期修复（已包含）

- 修复带空格的卸载路径被错误截断（如把 `C:\Program Files\...` 误判为 `C:\Program`）。
- 修复 `.bat / .cmd / .ps1` 等非 exe 启动器在含空格时无法启动。
- 改进残留项检测：新增运行中进程护栏，避免误删正在使用的软件（如微信）。
- 支持识别仅残留注册表项的「空壳」软件。
- 修复部分软件（如 OICPP IDE）安装大小显示为 0 的问题。
- 修复部分软件（如腾讯 QQ）图标显示错误。
- 修复扫描超大目录（如钉钉）或网络盘、含符号链接目录时界面卡死（未响应）的问题。
- 搜索支持中英文别名互搜（如搜「微信」可命中 WeChat，反之亦然）。
