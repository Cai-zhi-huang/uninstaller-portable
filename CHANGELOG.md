# 更新日志 (Changelog)

本文件记录「卸载管理器 (Uninstaller Manager)」各版本的改动说明。

---

## v0.0.1 (试用版) — 功能增强

> 本次更新聚焦「更安全、更高效、更好用」，共 10 项改进：

### 安全与删除
- **① 残留文件送回收站**：删除卸载残留文件时优先通过 `SHFileOperationW` 送入回收站（可撤销），仅在回收站不可用时回退为直接删除，避免误删无法恢复。
- **⑦ 系统关键项保护**：对 `SystemComponent`、系统发行商（Microsoft / Windows / Intel / Realtek 等）及名称含 driver / update / KB / Visual C++ / .NET 的条目自动灰显并标注「系统关键」，卸载时拦截，防止误卸系统组件。

### 批量与效率
- **② 操作后自动刷新**：卸载成功或删除残留后自动重扫软件列表并刷新界面，无需手动重载。
- **③ 批量操作**：表格支持多选（Ctrl/Shift），新增「批量卸载」与「批量删残留」按钮，可一次性处理多个条目（含系统关键项过滤与统一预览）。
- **⑩ 扫描进度细化**：扫描时进度标签显示当前正在扫描的盘符（如 `[扫描 D:]`），多盘/网络盘场景更直观。

### 卸载体验
- **④ 卸载前预览**：卸载前弹出预览框，列出目标软件及预计可释放的空间，确认后再执行。
- **⑧ 注册表定位**：右键菜单新增「在注册表中定位」，自动写入 `LastKey` 并拉起 `regedit` 跳转到该软件对应的卸载项。

### 搜索与导出
- **⑤ 搜索增强**：搜索框支持前缀匹配，并新增**拼音首字母哈希**（如「微信」可命中 `wx`、`WeChat`），中英文别名更易搜到。
- **⑥ 清单导出**：软件清单支持导出为 **CSV（RFC4180 转义）/ HTML（表格）/ TXT** 三种格式，便于归档与比对。

### 外观
- **⑨ 亮/暗主题**：菜单新增「视图 → 亮色 / 暗色」主题切换，选择通过 `QSettings` 持久化，重启后保持。

---

## v0.0.1 (试用版) — 早期修复（已包含）

- 修复带空格的卸载路径被错误截断（如把 `C:\Program Files\...` 误判为 `C:\Program`）。
- 修复 `.bat / .cmd / .ps1` 等非 exe 启动器在含空格时无法启动。
- 改进残留项检测：新增运行中进程护栏，避免误删正在使用的软件（如微信）。
- 支持识别仅残留注册表项的「空壳」软件。
- 修复部分软件（如 OICPP IDE）安装大小显示为 0 的问题。
- 修复部分软件（如腾讯 QQ）图标显示错误。
- 修复扫描超大目录（如钉钉）或网络盘、含符号链接目录时界面卡死（未响应）的问题。
- 搜索支持中英文别名互搜（如搜「微信」可命中 WeChat，反之亦然）。
